// StockWatch starter entry point.
// Dashboard calculations and interactions will be added in the planned build order.

const app = document.querySelector('#app');

if (app) {
  app.dataset.status = 'starter';
}
